# Build artifacts (regenerated by build.cmd / brcc32 / dcc32)
DeskTiler.exe
DeskTiler1.0.exe
DeskTiler.ico
DeskTiler.res
uMain.dcu
DeskTiler.dcu
DeskTiler.obj
*.identcache
*.stat
*.dproj.local
*.~*

# Generated by tools/bumpversion.ps1 on every build (build.number IS tracked)
uVersion.inc
DeskTilerVersion.rc

# Archival / packaging bundle
DeskTiler.zip
# 发布包: install/ 里是 DeskTiler.exe + 收款码 + README + 安装说明 和打好的 zip
# (由 tools/pack.ps1 生成; 二进制不进仓库, 要进仓库得 git add -f)
install/

# Outputs written into the project dir by tools/verify.ps1 / CLI self-test modes
verify.log
sorttest.txt
winlist.txt

# Stray leftover copies (unused)
DeskTiler_Icon.ico
300收款码.bmp

# Editor / OS noise
Thumbs.db
Desktop.ini
